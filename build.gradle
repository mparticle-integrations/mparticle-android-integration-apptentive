buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath 'com.mparticle:android-kit-plugin:' + project.version
    }
}

apply plugin: 'com.mparticle.kit'

android {
    defaultConfig {
        minSdkVersion 14
        resValue "string", "apptentive_distribution_version", project.version
    }
}

dependencies {
    compile 'com.apptentive:apptentive-android:4.0.3'
}
